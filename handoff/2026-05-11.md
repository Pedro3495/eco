# Handoff - 2026-05-11

## Contexto

Projeto Eco em `D:\projetos\Eco`, monorepo com backend Spring Boot e frontend Next.js.

Esta sessao focou em validar visualmente o frontend no navegador, corrigir quebras de UI e preparar o repositorio para commit/push.

## O que foi feito

- Corrigido bug visual onde conteudo do dashboard ficava invisivel por animacoes presas em `opacity: 0`.
- Corrigido card principal roxo sem padding quando `Card` recebia `gradient`.
- Adicionado timeout de 5s no client HTTP do frontend.
- Adicionado fallback mockado na tela de transacoes quando o backend esta desligado.
- Ajustado dashboard para mostrar dados mockados, badge `modo demonstracao`, graficos e ultimas transacoes.
- Ajustado FAB mobile para respeitar estado desabilitado em modo demo.
- Criado este fluxo de handoff em `handoff/` para registrar o estado por dia/conversa.
- `.firecrawl/` foi marcado como artefato local no `.gitignore`.

## Validacao

- `npm run build` em `frontend/` passou.
- Browser em `http://127.0.0.1:3000/` confirmou dashboard renderizando.
- Browser em `http://127.0.0.1:3000/transactions` confirmou 6 registros mockados e pagina `1 de 1`.

## Pendencias conhecidas

- Backend auth/JWT tem mudancas pendentes no workspace e deve ser validado com `backend/mvnw.cmd test`.
- Frontend ainda depende de backend real para criar/editar/excluir transacoes; quando API esta off, usa mocks.
- `.firecrawl/` ficou fora do versionamento por ser captura local de pesquisa.

## Proximo passo sugerido

Rodar a validacao backend completa e testar login/auth via HTTP real antes de continuar o roadmap de Auth/JWT.
